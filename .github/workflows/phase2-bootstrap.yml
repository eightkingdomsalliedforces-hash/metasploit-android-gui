name: Phase 2 Bootstrap

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  waiting-for-complete-source:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Phase 2 source upload is not complete yet.'
